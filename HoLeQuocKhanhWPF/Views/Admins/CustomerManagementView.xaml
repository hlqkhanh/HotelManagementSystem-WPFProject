<UserControl x:Class="HoLeQuocKhanhWPF.Views.Admins.CustomerManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Margin="5">
            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Width="200" Margin="5"/>
            <Button Content="Search" Command="{Binding SearchCommand}" Margin="5"/>
            <Button Content="Add" Command="{Binding AddCustomerCommand}" Margin="5"/>
            <Button Content="Edit" Command="{Binding EditCustomerCommand}" Margin="5"/>
            <Button Content="Delete" Command="{Binding DeleteCustomerCommand}" Margin="5"/>
        </StackPanel>

        <DataGrid Grid.Row="1" 
                  ItemsSource="{Binding Customers}" 
                  SelectedItem="{Binding SelectedCustomer, Mode=TwoWay}" 
                  AutoGenerateColumns="False" 
                  CanUserAddRows="False"
                  IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding CustomerID}"/>
                <DataGridTextColumn Header="Full Name" Binding="{Binding CustomerFullName}"/>
                <DataGridTextColumn Header="Telephone" Binding="{Binding Telephone}"/>
                <DataGridTextColumn Header="Email" Binding="{Binding EmailAddress}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>