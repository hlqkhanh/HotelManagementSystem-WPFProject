<Window x:Class="HoLeQuocKhanhWPF.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login - FUMiniHotelSystem" Height="500" Width="400"
        WindowStartupLocation="CenterScreen" WindowStyle="None"
        AllowsTransparency="True" Background="Transparent">
    <Border CornerRadius="12" Background="{StaticResource SecondaryBackgroundBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Button Content="&#x2715;" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="10" Click="Close_Click" Style="{StaticResource {x:Static ToolBar.ButtonStyleKey}}"/>

            <StackPanel Grid.Row="0" Margin="0,40,0,20">
                <TextBlock Text="FUMiniHotelSystem" HorizontalAlignment="Center" FontSize="28" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                <TextBlock Text="Welcome back! Please login to your account." HorizontalAlignment="Center" FontSize="14" Foreground="{StaticResource SecondaryTextBrush}"/>
            </StackPanel>

            <StackPanel Grid.Row="1" Margin="40,0">
                <TextBlock Text="Email Address" Style="{StaticResource LabelStyle}"/>
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource FormTextBoxStyle}"/>

                <TextBlock Text="Password" Style="{StaticResource LabelStyle}"/>
                <PasswordBox x:Name="PasswordBox" Style="{StaticResource FormPasswordBoxStyle}"/>

                <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource DangerBrush}" Margin="0,10" TextWrapping="Wrap"/>

                <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Style="{StaticResource BaseButtonStyle}" Margin="0,20,0,0"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>