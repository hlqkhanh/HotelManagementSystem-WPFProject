<Window x:Class="HoLeQuocKhanhWPF.Views.Dialogs.RoomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="Auto" Width="500" WindowStartupLocation="CenterScreen"
        SizeToContent="Height"
        Background="{StaticResource LightBrush}"
        WindowStyle="ToolWindow">
    <Window.Resources>
        <Style x:Key="DialogTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="5" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false" HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" Margin="5,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="DialogComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="{StaticResource LightBrush}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource SaveButtonStyle}">
            <Setter Property="Background" Value="#6C757D"/>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="{Binding Title, RelativeSource={RelativeSource AncestorType=Window}}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20" Foreground="{StaticResource DarkBrush}"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="20"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Room Number:" VerticalAlignment="Center" Margin="0,0,10,10" Foreground="{StaticResource DarkBrush}"/>
                <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Room.RoomNumber, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Description:" VerticalAlignment="Top" Margin="0,5,10,10" Foreground="{StaticResource DarkBrush}"/>
                <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Room.RoomDescription, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"
                         TextWrapping="Wrap" AcceptsReturn="True" MinHeight="80" VerticalScrollBarVisibility="Auto"/>
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Max Capacity:" VerticalAlignment="Center" Margin="0,0,10,10" Foreground="{StaticResource DarkBrush}"/>
                <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Room.RoomMaxCapacity, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Price Per Date:" VerticalAlignment="Center" Margin="0,0,10,10" Foreground="{StaticResource DarkBrush}"/>
                <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Room.RoomPricePerDate, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource DialogTextBoxStyle}" Margin="0,0,0,10"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Text="Room Type:" VerticalAlignment="Center" Margin="0,0,10,10" Foreground="{StaticResource DarkBrush}"/>
                <ComboBox Grid.Row="4" Grid.Column="1"
                           ItemsSource="{Binding RoomTypes}"
                           DisplayMemberPath="RoomTypeName"
                           SelectedValuePath="RoomTypeID" SelectedValue="{Binding Room.RoomTypeID, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource DialogComboBoxStyle}" Margin="0,0,0,10"/>

                <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Save" Command="{Binding SaveCommand}" Style="{StaticResource SaveButtonStyle}"
                            CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"/>
                    <Button Content="Cancel" IsCancel="True" Style="{StaticResource CancelButtonStyle}" Margin="10,0,0,0"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Grid>
</Window>