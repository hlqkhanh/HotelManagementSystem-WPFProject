<Window x:Class="HoLeQuocKhanhWPF.Views.Dialogs.RoomDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Room Details" Height="450" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Room Number:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Room.RoomNumber, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Description:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Room.RoomDescription, UpdateSourceTrigger=PropertyChanged}" TextWrapping="Wrap" AcceptsReturn="True" Height="60"/>

        <Label Grid.Row="2" Grid.Column="0" Content="Max Capacity:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Room.RoomMaxCapacity, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Price Per Date:"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Room.RoomPricePerDate, UpdateSourceTrigger=PropertyChanged}"/>

        <Label Grid.Row="4" Grid.Column="0" Content="Room Type:"/>
        <ComboBox Grid.Row="4" Grid.Column="1" 
                  ItemsSource="{Binding RoomTypes}" 
                  DisplayMemberPath="RoomTypeName"
                  SelectedValuePath="RoomTypeId"
                  SelectedValue="{Binding Room.RoomTypeId}"/>

        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Bottom">
            <Button Content="Save" Command="{Binding SaveCommand}" CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}" Margin="5"/>
            <Button Content="Cancel" IsCancel="True" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>