<Window x:Class="HoLeQuocKhanhWPF.Views.Dialogs.ChangePasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Password" Height="Auto" Width="450" WindowStartupLocation="CenterScreen"
        SizeToContent="Height" Background="{StaticResource LightBrush}"
        WindowStyle="ToolWindow">
    <Window.Resources>
        <Style x:Key="DialogPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="5,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="{StaticResource LightBrush}"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="80"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="CancelButtonStyle" TargetType="Button" BasedOn="{StaticResource SaveButtonStyle}">
            <Setter Property="Background" Value="#6C757D"/>
        </Style>

    </Window.Resources>

    <Grid Margin="20">
        <StackPanel>
            <TextBlock Text="🔒 Change Your Password" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20" Foreground="{StaticResource DarkBrush}"/>

            <TextBlock Text="Current Password:" Foreground="{StaticResource DarkBrush}" Margin="0,0,0,5"/>
            <PasswordBox Name="CurrentPasswordBox" Style="{StaticResource DialogPasswordBoxStyle}" Margin="0,0,0,15"/>

            <TextBlock Text="New Password:" Foreground="{StaticResource DarkBrush}" Margin="0,0,0,5"/>
            <PasswordBox Name="NewPasswordBox" Style="{StaticResource DialogPasswordBoxStyle}" Margin="0,0,0,15"/>

            <TextBlock Text="Confirm New Password:" Foreground="{StaticResource DarkBrush}" Margin="0,0,0,5"/>
            <PasswordBox Name="ConfirmPasswordBox" Style="{StaticResource DialogPasswordBoxStyle}" Margin="0,0,0,10"/>

            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentBrush}" Margin="0,5,0,15" TextWrapping="Wrap" MinHeight="20"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Save" Style="{StaticResource SaveButtonStyle}">
                    <Button.CommandParameter>
                        <MultiBinding Converter="{StaticResource PasswordBoxesConverter}">
                            <Binding ElementName="CurrentPasswordBox"/>
                            <Binding ElementName="NewPasswordBox"/>
                            <Binding ElementName="ConfirmPasswordBox"/>
                        </MultiBinding>
                    </Button.CommandParameter>
                    <Button.Command>
                        <Binding Path="SavePasswordCommand"/>
                    </Button.Command>
                </Button>
                <Button Content="Cancel" Style="{StaticResource CancelButtonStyle}" IsCancel="True" Margin="10,0,0,0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>