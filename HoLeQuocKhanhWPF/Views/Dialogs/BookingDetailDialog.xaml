<Window x:Class="HoLeQuocKhanhWPF.Views.Dialogs.BookingDetailDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Booking Details" Height="300" Width="500"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <DataGrid ItemsSource="{Binding BookingDetails}" IsReadOnly="True" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Room Number" Binding="{Binding Room.RoomNumber}"/>
                <DataGridTextColumn Header="Start Date" Binding="{Binding StartDate, StringFormat='dd/MM/yyyy'}"/>
                <DataGridTextColumn Header="End Date" Binding="{Binding EndDate, StringFormat='dd/MM/yyyy'}"/>
                <DataGridTextColumn Header="Price" Binding="{Binding ActualPrice, StringFormat=C}"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>