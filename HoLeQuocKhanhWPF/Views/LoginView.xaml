<Window x:Class="HoLeQuocKhanhWPF.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login" Height="300" Width="400"
        WindowStartupLocation="CenterScreen">
    <Grid Background="{StaticResource SecondaryBrush}">
        <StackPanel VerticalAlignment="Center" Margin="20">
            <Label Content="Email:" />
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" />
            <Label Content="Password:" Margin="0,10,0,0" />
            <PasswordBox Name="PasswordBox" />
            <Button Content="Login" Command="{Binding LoginCommand}" CommandParameter="{Binding ElementName=PasswordBox}" Margin="0,20,0,0"/>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="{StaticResource AccentBrush}" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>
    </Grid>
</Window>