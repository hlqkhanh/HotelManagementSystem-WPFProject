<Window x:Class="HoLeQuocKhanhWPF.Views.Customers.CustomerMainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Customer Portal" Height="650" Width="950" WindowStartupLocation="CenterScreen"
        Background="{StaticResource SecondaryBrush}">
    <Window.Resources>
        <SolidColorBrush x:Key="PrimaryBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#F0F0F0"/>
        <SolidColorBrush x:Key="LightBrush" Color="White"/>
        <SolidColorBrush x:Key="DarkBrush" Color="#333333"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#E53935"/>
        <SolidColorBrush x:Key="SidebarForegroundBrush" Color="#E0E0E0"/>
        <SolidColorBrush x:Key="SidebarHoverBrush" Color="#455A64"/>
        <SolidColorBrush x:Key="SidebarSelectedBrush" Color="#005A9C"/>

        <LinearGradientBrush x:Key="CustomerSidebarGradientBrush" StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#1A2980" Offset="0.0"/>
            <GradientStop Color="#26D0CE" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="SidebarButtonStyle" TargetType="RadioButton">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource SidebarForegroundBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="GroupName" Value="CustomerNavigationGroup"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="RadioButton">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarHoverBrush}"/>
                            </Trigger>
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource SidebarSelectedBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource LightBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource LightBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="Margin" Value="10,5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5" Padding="{TemplateBinding Padding}">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="220"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource CustomerSidebarGradientBrush}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="{Binding WelcomeMessage}"
                           FontSize="16" FontWeight="SemiBold" Foreground="{StaticResource LightBrush}"
                           TextWrapping="Wrap" TextAlignment="Center" Margin="15,20"/>

                <StackPanel Grid.Row="1" Margin="10,10,10,10">
                    <RadioButton Content="👤 My Profile" Command="{Binding ShowProfileCommand}" Style="{StaticResource SidebarButtonStyle}" IsChecked="True"/>
                    <RadioButton Content="📜 Booking History" Command="{Binding ShowBookingHistoryCommand}" Style="{StaticResource SidebarButtonStyle}"/>
                    <RadioButton Content="🏨 Book a Room" Command="{Binding BookRoomCommand}" Style="{StaticResource SidebarButtonStyle}"/>
                </StackPanel>

                <Button Grid.Row="2" Content="Logout"
                         Command="{Binding LogoutCommand}"
                         CommandParameter="{Binding RelativeSource={RelativeSource AncestorType=Window}}"
                         Style="{StaticResource LogoutButtonStyle}"
                         Margin="15" VerticalAlignment="Bottom"/>
            </Grid>
        </Border>

        <Border Grid.Column="1" Background="{StaticResource LightBrush}" CornerRadius="10,0,0,10" Padding="20">
            <ContentControl Content="{Binding CurrentViewModel}"/>
        </Border>

    </Grid>
</Window>