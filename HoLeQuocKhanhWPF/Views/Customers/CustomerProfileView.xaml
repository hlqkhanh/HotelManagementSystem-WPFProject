<UserControl x:Class="HoLeQuocKhanhWPF.Views.Customers.CustomerProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="600"
             Background="{StaticResource LightBrush}">
    <UserControl.Resources>
        <Style x:Key="ProfileTextBoxStyle" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5" SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="false"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden"
                                          Margin="5,0"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsReadOnly" Value="True">
                                <Setter Property="Background" Value="{StaticResource SecondaryBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource DarkBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ProfileDatePickerStyle" TargetType="DatePicker">
            <Setter Property="Padding" Value="8,5"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource DataGridBorderBrush}"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <Style x:Key="SaveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#28A745"/>
            <Setter Property="Foreground" Value="{StaticResource LightBrush}"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="MinWidth" Value="120"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.85"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Opacity" Value="0.7"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ChangePasswordButtonStyle" TargetType="Button" BasedOn="{StaticResource SaveButtonStyle}">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="MinWidth" Value="150"/>
        </Style>

    </UserControl.Resources>

    <Grid Margin="15">
        <StackPanel MaxWidth="500">
            <TextBlock Text="👤 My Profile" FontSize="24" FontWeight="Bold" Margin="0,0,0,25" Foreground="{StaticResource DarkBrush}"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" MinWidth="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Full Name:" VerticalAlignment="Center" Margin="5" Foreground="{StaticResource DarkBrush}"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding CurrentCustomer.CustomerFullName, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ProfileTextBoxStyle}" Margin="5,5,5,10"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Telephone:" VerticalAlignment="Center" Margin="5" Foreground="{StaticResource DarkBrush}"/>
                <TextBox Grid.Row="1" Grid.Column="1"
                         Text="{Binding CurrentCustomer.Telephone, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ProfileTextBoxStyle}" Margin="5,5,5,10"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Email Address:" VerticalAlignment="Center" Margin="5" Foreground="{StaticResource DarkBrush}"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding CurrentCustomer.EmailAddress, UpdateSourceTrigger=PropertyChanged}"
                         Style="{StaticResource ProfileTextBoxStyle}" Margin="5,5,5,10"
                         IsReadOnly="True"/>
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Birthday:" VerticalAlignment="Center" Margin="5" Foreground="{StaticResource DarkBrush}"/>
                <DatePicker Grid.Row="3" Grid.Column="1"
                            SelectedDate="{Binding CurrentCustomer.CustomerBirthday, UpdateSourceTrigger=PropertyChanged}"
                            Style="{StaticResource ProfileDatePickerStyle}" Margin="5,5,5,10"/>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="125,25,0,0">
                <Button Content="Save Changes" Command="{Binding SaveChangesCommand}"
                        Style="{StaticResource SaveButtonStyle}" />
                <Button Content="Change Password" Command="{Binding ChangePasswordCommand}"
                        Style="{StaticResource ChangePasswordButtonStyle}" Margin="15,0,0,0"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>